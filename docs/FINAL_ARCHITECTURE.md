# n8n-library.com 最终实施方案 v2.0

> **Unofficial Community** - 非官方社区 n8n 模板库
>
> 数据来源:
> - https://github.com/enescingoz/awesome-n8n-templates (288个精选)
> - https://github.com/Zie619/n8n-workflows (2,061个社区)

---

## 一、项目定位

### 1.1 竞品分析

| 平台 | 模板数 | 定位 | 商业模式 |
|------|--------|------|----------|
| [n8n.io/workflows](https://n8n.io/workflows/) | 7,164 | 官方库，Creator Program | 免费 + 付费 |
| [haveworkflow.com](https://haveworkflow.com/) | ~500 | 多平台 Marketplace | 免费 + 付费模板 |
| [n8nmarket.com](https://n8nmarket.com/) | ~200 | Premium 定制服务 | 付费为主 |

### 1.2 差异化定位

| 维度 | 竞品 | n8n-library.com |
|------|------|-----------------|
| **数据来源** | 用户提交审核 | GitHub 开源聚合 (自动化) |
| **目标用户** | 买家/卖家 | 学习者/开发者/探索者 |
| **核心价值** | 交易变现 | **发现 + 学习 + SEO 长尾** |
| **更新频率** | 人工审核 | GitHub Action 自动同步 |
| **成本** | 平台抽成 | 完全免费 |

---

## 二、技术架构

### 2.1 架构概览

```
┌─────────────────────────────────────────────────────────────────┐
│                        BUILD TIME                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌────────────────┐   ┌────────────────┐                       │
│  │ awesome-n8n    │   │ n8n-workflows  │   (Git Submodules)    │
│  │ (288 精选)     │   │ (2,061 社区)   │                       │
│  └───────┬────────┘   └───────┬────────┘                       │
│          │                    │                                 │
│          └─────────┬──────────┘                                 │
│                    ▼                                            │
│          ┌─────────────────────┐                                │
│          │   ETL Script        │   scripts/build-data.js       │
│          │   - 解析 JSON       │                                │
│          │   - 提取元数据      │                                │
│          │   - 自动分类        │                                │
│          │   - 图标映射        │                                │
│          └──────────┬──────────┘                                │
│                     ▼                                           │
│          ┌─────────────────────┐                                │
│          │ public/data/        │                                │
│          │ ├── index.json      │   (~2MB, gzip ~400KB)         │
│          │ └── workflows/*.json│   (详情按需加载)               │
│          └──────────┬──────────┘                                │
│                     ▼                                           │
│          ┌─────────────────────┐                                │
│          │   Next.js SSG       │   output: 'export'            │
│          │   ~2,500 静态页面    │                                │
│          └──────────┬──────────┘                                │
│                     ▼                                           │
│          ┌─────────────────────┐                                │
│          │   /out 目录         │   纯静态 HTML/CSS/JS           │
│          └─────────────────────┘                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        RUNTIME                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│         ┌─────────────────────────────────────┐                 │
│         │      Cloudflare Pages (推荐)        │                 │
│         │  ┌─────────────────────────────┐    │                 │
│         │  │  GitHub 推送自动触发构建     │    │                 │
│         │  │  全球 Anycast CDN 分发      │    │                 │
│         │  │  自动 HTTPS + 缓存优化      │    │                 │
│         │  └─────────────────────────────┘    │                 │
│         └─────────────────────────────────────┘                 │
│                          OR                                     │
│         ┌─────────────────────────────────────┐                 │
│         │      VPS + Nginx (备选)             │                 │
│         │  ┌─────────────────────────────┐    │                 │
│         │  │  本地构建 -> rsync 上传      │    │                 │
│         │  │  Nginx 托管静态文件          │    │                 │
│         │  │  Cloudflare 前置加速        │    │                 │
│         │  └─────────────────────────────┘    │                 │
│         └─────────────────────────────────────┘                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 层级 | 技术 | 选型理由 |
|------|------|----------|
| **框架** | Next.js 14 (App Router) | SSG 最佳支持，SEO 友好 |
| **样式** | Tailwind CSS + shadcn/ui | 快速开发，组件丰富 |
| **搜索** | Fuse.js (客户端) | 2,300条 <10ms，零服务器压力 |
| **数据** | 静态 JSON + Git Submodules | 无数据库，自动同步上游 |
| **图标** | simple-icons + n8n CDN fallback | 覆盖率高，统一风格 |
| **部署** | Cloudflare Pages (主) / VPS (备) | 全球 CDN，自动构建 |

### 2.3 为什么选静态方案？

| 方案 | 2,300 模板 | 搜索延迟 | 月成本 | 维护 |
|------|-----------|----------|--------|------|
| **静态 JSON + Fuse.js** | ✅ | <10ms | $0 | 零 |
| PostgreSQL + API | ✅ | 50-200ms | $10+ | 中 |
| Algolia 托管搜索 | ✅ | <50ms | $0-100 | 低 |
| Elasticsearch | ✅ | <30ms | $50+ | 高 |

**结论**: 数据量 <10,000，静态方案完胜。

---

## 三、目录结构

```
n8n-library/
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Action: 自动构建部署
│
├── data/
│   └── raw/                    # Git Submodules (原始数据)
│       ├── awesome-n8n/        # git submodule
│       └── n8n-workflows/      # git submodule
│
├── scripts/
│   └── build-data.js           # ETL 脚本
│
├── public/
│   ├── data/
│   │   ├── index.json          # 搜索索引 (构建时生成)
│   │   └── workflows/          # 详情 JSON (构建时生成)
│   │       └── [slug].json
│   ├── icons/                  # 节点图标 SVG
│   └── og/                     # Open Graph 图片 (可选)
│
├── src/
│   ├── app/
│   │   ├── layout.tsx          # 根布局
│   │   ├── page.tsx            # 首页
│   │   ├── search/
│   │   │   └── page.tsx        # 搜索页 (CSR)
│   │   ├── category/
│   │   │   └── [slug]/
│   │   │       └── page.tsx    # 分类页 (SSG)
│   │   ├── integration/
│   │   │   └── [slug]/
│   │   │       └── page.tsx    # 集成页 (SSG)
│   │   └── workflow/
│   │       └── [slug]/
│   │           └── page.tsx    # 详情页 (SSG)
│   │
│   ├── components/
│   │   ├── ui/                 # shadcn/ui 组件
│   │   ├── WorkflowCard.tsx    # 模板卡片
│   │   ├── SearchBar.tsx       # 搜索框
│   │   ├── NodeIcons.tsx       # 节点图标组
│   │   ├── NodeFlow.tsx        # 简化流程图
│   │   ├── CodeBlock.tsx       # JSON 代码块
│   │   └── CategoryGrid.tsx    # 分类网格
│   │
│   ├── lib/
│   │   ├── data.ts             # 数据加载
│   │   ├── search.ts           # Fuse.js 封装
│   │   ├── categories.ts       # 分类配置
│   │   └── icons.ts            # 图标映射
│   │
│   └── styles/
│       └── globals.css         # Tailwind + 自定义样式
│
├── next.config.js              # Next.js 配置
├── tailwind.config.js          # Tailwind 配置
├── package.json
├── .gitmodules                 # Submodules 配置
└── README.md
```

---

## 四、页面路由规划

### 4.1 路由结构

```
n8n-library.com/
│
├── /                           # 首页
│   ├── Hero + 统计数字
│   ├── 分类快捷入口 (8个)
│   ├── 精选推荐 (12个, source=awesome)
│   ├── 热门集成 (Top 20 icons)
│   └── 最新添加 (12个)
│
├── /search                     # 搜索页 (CSR)
│   ├── 即时搜索 (Fuse.js)
│   ├── 筛选: 分类 / 来源 / 节点数
│   └── 虚拟滚动列表
│
├── /category/[slug]            # 分类页 (SSG)
│   ├── /category/ai-automation
│   ├── /category/communication
│   ├── /category/productivity
│   ├── /category/devops
│   ├── /category/crm-sales
│   ├── /category/e-commerce
│   ├── /category/data-processing
│   └── /category/utilities
│
├── /integration/[slug]         # 集成页 (SSG, Top 50)
│   ├── /integration/openai
│   ├── /integration/telegram
│   ├── /integration/google-sheets
│   ├── /integration/slack
│   └── ...
│
├── /workflow/[slug]            # 详情页 (SSG, ~2,300个)
│   ├── 标题 + 描述
│   ├── 节点流程可视化
│   ├── 集成图标列表
│   ├── JSON 代码块 (一键复制)
│   ├── 导入教程
│   └── 相关推荐
│
└── /about                      # 关于页
    └── Unofficial 声明 + 数据来源
```

### 4.2 页面生成策略

| 页面 | 渲染方式 | generateStaticParams | 数量 |
|------|----------|----------------------|------|
| 首页 | SSG | - | 1 |
| 搜索页 | CSR | - | 1 |
| 分类页 | SSG | 8 个分类 slug | 8 |
| 集成页 | SSG | Top 50 热门节点 | 50 |
| 详情页 | SSG | 所有 workflow slug | ~2,300 |
| 关于页 | SSG | - | 1 |

**总计: ~2,360 个静态页面**

---

## 五、ETL 数据处理

### 5.1 分类映射配置

```javascript
// Awesome 库: 文件夹 -> 分类
const AWESOME_CATEGORY_MAP = {
  'AI_Research_RAG_and_Data_Analysis': 'ai-automation',
  'OpenAI_and_LLMs': 'ai-automation',
  'Gmail_and_Email_Automation': 'communication',
  'Telegram': 'communication',
  'Discord': 'communication',
  'Slack': 'communication',
  'WhatsApp': 'communication',
  'Google_Drive_and_Google_Sheets': 'productivity',
  'Notion': 'productivity',
  'Airtable': 'productivity',
  'Forms_and_Surveys': 'productivity',
  'Database_and_Storage': 'data-processing',
  'PDF_and_Document_Processing': 'data-processing',
  'Instagram_Twitter_Social_Media': 'communication',
  'WordPress': 'productivity',
  'HR_and_Recruitment': 'crm-sales',
  'devops': 'devops',
  'Other_Integrations_and_Use_Cases': 'utilities',
  'Other': 'utilities',
};

// 节点关键词 -> 分类 (Workflow 库用)
const NODE_CATEGORY_RULES = {
  'ai-automation': [
    'openai', 'anthropic', 'langchain', 'llm', 'gpt', 'ai',
    'chatgpt', 'ollama', 'huggingface', 'cohere', 'gemini'
  ],
  'communication': [
    'telegram', 'discord', 'slack', 'whatsapp', 'email',
    'gmail', 'smtp', 'twilio', 'sendgrid', 'mailchimp'
  ],
  'productivity': [
    'notion', 'airtable', 'googlesheets', 'todoist', 'trello',
    'asana', 'clickup', 'monday', 'googledocs', 'dropbox'
  ],
  'devops': [
    'ssh', 'docker', 'github', 'gitlab', 'jenkins', 'aws',
    'kubernetes', 'terraform', 'ansible', 'cicd'
  ],
  'crm-sales': [
    'hubspot', 'pipedrive', 'salesforce', 'zoho', 'intercom',
    'zendesk', 'freshdesk', 'crisp'
  ],
  'e-commerce': [
    'shopify', 'woocommerce', 'stripe', 'paypal', 'magento',
    'bigcommerce', 'square'
  ],
  'data-processing': [
    'postgres', 'mysql', 'mongodb', 'redis', 'elasticsearch',
    'csv', 'pdf', 'excel', 'json', 'xml', 'supabase'
  ],
};
```

### 5.2 元数据结构

```typescript
interface WorkflowMeta {
  // 标识
  id: string;                    // 唯一 ID
  slug: string;                  // URL slug

  // 基本信息
  name: string;                  // 显示名称
  description: string;           // 描述 (提取或生成)

  // 来源与质量
  source: 'awesome' | 'community';  // 数据来源
  quality: 1 | 2 | 3 | 4 | 5;       // 质量评分
  sourceUrl: string;                 // GitHub 原始链接

  // 分类
  category: string;              // 主分类 slug
  tags: string[];                // 标签数组

  // 节点信息
  nodes: string[];               // 节点类型列表 (简化)
  nodeCount: number;             // 节点总数
  integrations: Integration[];   // 集成列表 (带图标)

  // 文件路径
  filePath: string;              // 原始 JSON 路径
}

interface Integration {
  name: string;                  // 显示名称
  slug: string;                  // URL slug
  icon: string;                  // 图标文件名或 URL
}
```

### 5.3 质量评分规则

| 来源 | 基础分 | 加分规则 |
|------|--------|----------|
| awesome-n8n-templates | 4 | +1 有 StickyNote 描述 |
| n8n-workflows | 2 | +1 名称有意义 (非纯数字), +1 节点数>5, +1 有 description |

### 5.4 图标映射

```javascript
// 节点类型 -> simple-icons slug
const ICON_MAP = {
  'telegram': 'telegram',
  'telegramTrigger': 'telegram',
  'openAi': 'openai',
  'googleSheets': 'googlesheets',
  'gmail': 'gmail',
  'slack': 'slack',
  'discord': 'discord',
  'notion': 'notion',
  'airtable': 'airtable',
  'github': 'github',
  'postgres': 'postgresql',
  'mysql': 'mysql',
  'mongodb': 'mongodb',
  'redis': 'redis',
  'stripe': 'stripe',
  'shopify': 'shopify',
  'webhook': 'webhook',  // 自定义图标
  'httpRequest': 'curl', // 或自定义
  // ... 其他映射
};

// Fallback: 使用 n8n 官方 CDN
const N8N_ICON_CDN = 'https://n8n.io/integrations/';
```

---

## 六、页面设计

### 6.1 首页布局

```
┌─────────────────────────────────────────────────────────────────┐
│  [Logo]  n8n Library    [Search...]           [GitHub] [About]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│         2,300+ Free n8n Workflow Templates                      │
│         ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                      │
│         Unofficial community collection from GitHub             │
│                                                                 │
│                [Browse All]   [Random Pick]                     │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  Categories                                                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              │
│  │  🤖 AI  │ │ 💬 Comm │ │ 📊 Prod │ │ 🔧 Dev  │              │
│  │   523   │ │   412   │ │   389   │ │   201   │              │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘              │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              │
│  │ 💼 CRM  │ │ 🛒 Shop │ │ 💾 Data │ │ 🔌 Other│              │
│  │   156   │ │   134   │ │   298   │ │   187   │              │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘              │
├─────────────────────────────────────────────────────────────────┤
│  ⭐ Featured Templates (from awesome-n8n)                       │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐        │
│  │ [TG][AI][📧]  │ │ [📊][Notion]  │ │ [GH][Slack]   │        │
│  │               │ │               │ │               │        │
│  │ AI Telegram   │ │ Sheet to      │ │ GitHub PR     │        │
│  │ Chatbot       │ │ Notion Sync   │ │ Notifier      │        │
│  │               │ │               │ │               │        │
│  │ ⭐Featured 12n│ │ ⭐Featured 8n │ │ ⭐Featured 6n │        │
│  └───────────────┘ └───────────────┘ └───────────────┘        │
│                                        [View All Featured →]    │
├─────────────────────────────────────────────────────────────────┤
│  🔌 Popular Integrations                                        │
│  [OpenAI] [Telegram] [Gmail] [Sheets] [Notion] [Slack]         │
│  [Discord] [Airtable] [GitHub] [Postgres] [Shopify] [+38]      │
├─────────────────────────────────────────────────────────────────┤
│  Footer                                                         │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  Unofficial Community Project | Not affiliated with n8n GmbH   │
│  Data sources: awesome-n8n-templates | n8n-workflows            │
│  [GitHub] [Twitter]                              Last sync: 2h  │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 详情页布局

```
┌─────────────────────────────────────────────────────────────────┐
│  ← Back to AI Automation                     [GitHub] [About]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Telegram AI Chatbot with OpenAI GPT-4                          │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                          │
│                                                                 │
│  AI-powered chatbot that responds to Telegram messages using    │
│  OpenAI's GPT-4. Supports text chat, image generation, and      │
│  multilingual responses.                                        │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Category: AI Automation │ Nodes: 12 │ ⭐ Featured       │   │
│  │ Source: awesome-n8n-templates                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  Workflow Flow                                                  │
│  ┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐         │
│  │  [TG]  │ →  │  [⚙️]  │ →  │  [AI]  │ →  │  [TG]  │         │
│  │Trigger │    │Settings│    │ OpenAI │    │  Send  │         │
│  └────────┘    └────────┘    └────────┘    └────────┘         │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  Integrations                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                       │
│  │ [TG]     │ │ [AI]     │ │ [⚡]     │                       │
│  │ Telegram │ │ OpenAI   │ │ Webhook  │                       │
│  └──────────┘ └──────────┘ └──────────┘                       │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  [📋 Copy JSON]    [📥 Download]    [🔗 View on GitHub]         │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ {                                                        │   │
│  │   "name": "Telegram AI Chatbot",                        │   │
│  │   "nodes": [...],                                       │   │
│  │   "connections": {...}                                  │   │
│  │ }                                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  ▼ How to Import                                                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1. Copy the JSON above                                   │   │
│  │ 2. In n8n, click "Workflows" → "Import from URL/File"   │   │
│  │ 3. Paste the JSON and click "Import"                    │   │
│  │ 4. Configure your credentials (Telegram, OpenAI)        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  Related Workflows                                              │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐        │
│  │ Discord AI    │ │ Slack GPT     │ │ WhatsApp Bot  │        │
│  │ Chatbot       │ │ Assistant     │ │ with AI       │        │
│  └───────────────┘ └───────────────┘ └───────────────┘        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.3 卡片组件

```
┌─────────────────────────────────┐
│  [TG] [AI] [📧] [+2]           │  ← 集成图标 (最多显示4个)
├─────────────────────────────────┤
│                                 │
│  Telegram AI Chatbot with      │  ← 标题 (2行截断)
│  OpenAI GPT-4                  │
│                                 │
│  AI-powered chatbot that       │  ← 描述 (2行截断)
│  responds to messages...       │
│                                 │
├─────────────────────────────────┤
│  ⭐ Featured  │  12 nodes      │  ← 元信息
└─────────────────────────────────┘
```

---

## 七、SEO 策略

### 7.1 页面 Title 模板

| 页面 | Title 模板 |
|------|-----------|
| 首页 | `2,300+ Free n8n Workflow Templates - n8n Library` |
| 分类页 | `{Category} n8n Templates & Workflows - n8n Library` |
| 集成页 | `n8n {Integration} Workflow Templates - n8n Library` |
| 详情页 | `{Workflow Name} - Free n8n Template` |
| 搜索页 | `Search n8n Templates - n8n Library` |

### 7.2 Meta Description 模板

| 页面 | Description 模板 |
|------|-----------------|
| 首页 | `Discover 2,300+ free n8n workflow templates. Browse AI automation, email marketing, CRM integrations and more. Copy & import in seconds.` |
| 分类页 | `Browse {count} free n8n {category} templates. Ready-to-use workflows for {use_cases}. One-click import.` |
| 集成页 | `{count} n8n workflow templates using {Integration}. Automate {use_cases} with ready-made workflows.` |
| 详情页 | `{description} Free n8n template with {nodeCount} nodes. Uses {integrations}. Copy JSON and import instantly.` |

### 7.3 结构化数据

```json
// 详情页 Schema.org
{
  "@context": "https://schema.org",
  "@type": "SoftwareSourceCode",
  "name": "Telegram AI Chatbot with OpenAI",
  "description": "AI-powered chatbot workflow...",
  "programmingLanguage": "n8n workflow JSON",
  "runtimePlatform": "n8n",
  "license": "MIT",
  "isAccessibleForFree": true,
  "author": {
    "@type": "Organization",
    "name": "n8n Community"
  }
}
```

### 7.4 技术 SEO

- ✅ 静态 HTML (SSG) - 极速首屏
- ✅ 语义化 HTML (h1/h2/h3 层级正确)
- ✅ next-sitemap 自动生成 sitemap.xml
- ✅ robots.txt 配置
- ✅ Canonical URLs (避免重复内容)
- ✅ Open Graph + Twitter Cards
- ✅ 移动端响应式优先
- ✅ Core Web Vitals 优化

---

## 八、部署方案

### 8.1 方案对比

| 维度 | Cloudflare Pages (推荐) | VPS + Nginx (备选) |
|------|------------------------|-------------------|
| **构建** | 自动 (GitHub push 触发) | 手动/CI 构建后上传 |
| **性能** | 全球 Anycast CDN，极快 | 取决于 VPS 位置 |
| **HTTPS** | 全自动 | 需配置 Certbot |
| **成本** | $0 (免费额度足够) | $0 (已有 VPS) |
| **维护** | 零维护 | 需管理 Nginx/Docker |
| **更新** | 推送即更新 | 需 rsync/部署脚本 |

### 8.2 Cloudflare Pages 配置

```yaml
# 构建设置
Framework preset: Next.js (Static HTML Export)
Build command: npm run build
Build output directory: out

# 环境变量 (可选)
NODE_VERSION: 18
```

**package.json 配置**:
```json
{
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/build-data.js && next build",
    "start": "next start"
  }
}
```

**next.config.js**:
```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',           // 纯静态导出
  images: {
    unoptimized: true,        // CF Pages 不支持 Image API
  },
  trailingSlash: true,        // URL 末尾加斜杠
};

module.exports = nextConfig;
```

### 8.3 VPS 备选方案 (符合 SOP)

```yaml
# docker-compose.yml
version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: n8n_library_web
    restart: unless-stopped
    ports:
      - "3002:80"
    volumes:
      - ./data/dist:/usr/share/nginx/html:ro
      - ./config/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./logs:/var/log/nginx
```

```nginx
# config/nginx.conf
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript;

    location /_next/static {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location / {
        try_files $uri $uri.html $uri/index.html =404;
        expires 1h;
    }

    error_page 404 /404.html;
}
```

---

## 九、GitHub Action 自动化

```yaml
# .github/workflows/deploy.yml
name: Build and Deploy

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 0 * * 0'  # 每周日同步上游

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive  # 拉取 Git Submodules

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Update submodules
        run: |
          git submodule update --remote --merge

      - name: Build
        run: npm run build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          projectName: n8n-library
          directory: out
```

---

## 十、执行路线图

### Phase 1: 数据地基 (Day 1)

- [ ] 初始化 Next.js 项目
- [ ] 配置 Git Submodules
- [ ] 编写 ETL 脚本 `build-data.js`
- [ ] 验证分类映射准确率
- [ ] 生成 `index.json` 测试数据

### Phase 2: 核心开发 (Day 2-4)

- [ ] 实现 `WorkflowCard` 组件
- [ ] 实现 `NodeIcons` 组件 + 图标映射
- [ ] 详情页 `/workflow/[slug]` (SSG)
- [ ] 首页 `/` (SSG)
- [ ] 分类页 `/category/[slug]` (SSG)

### Phase 3: 搜索与优化 (Day 5-6)

- [ ] 集成 Fuse.js 搜索
- [ ] 搜索页 `/search` (CSR)
- [ ] 集成页 `/integration/[slug]` (SSG)
- [ ] SEO 优化 (sitemap, schema, meta)
- [ ] 移动端响应式

### Phase 4: 部署上线 (Day 7)

- [ ] 配置 Cloudflare Pages
- [ ] 配置 GitHub Action
- [ ] 绑定域名 n8n-library.com
- [ ] 验证全流程
- [ ] 提交 Google Search Console

### Phase 5: 持续优化 (持续)

- [ ] 监控 Analytics
- [ ] A/B 测试首页
- [ ] 添加内容页 (教程、最佳实践)
- [ ] 社区反馈迭代

---

## 十一、成本估算

| 项目 | 月成本 |
|------|--------|
| Cloudflare Pages | $0 |
| 域名 (n8n-library.com) | ~$1 |
| GitHub (公开仓库) | $0 |
| **总计** | **~$1/月** |

---

## 十二、风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 版权投诉 | 低 | 中 | Footer 声明 Unofficial + 来源链接，DMCA 响应机制 |
| 上游数据变更 | 中 | 低 | 每周 GitHub Action 自动同步 |
| n8n 官方竞争 | 中 | 中 | 差异化定位 (学习/发现 vs 交易)，长尾 SEO |
| 流量突增 | 低 | 低 | 全静态 + Cloudflare CDN，无服务器压力 |

---

## 附录: .gitmodules 配置

```ini
[submodule "data/raw/awesome-n8n"]
    path = data/raw/awesome-n8n
    url = https://github.com/enescingoz/awesome-n8n-templates.git

[submodule "data/raw/n8n-workflows"]
    path = data/raw/n8n-workflows
    url = https://github.com/Zie619/n8n-workflows.git
```

---

**文档版本**: v2.0
**最后更新**: 2024-11-30
**作者**: Claude (AI Assistant)
